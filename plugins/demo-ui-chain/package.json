{
  "name": "polaris-demo-ui-chain",
  "version": "0.1.0",
  "private": true,
  "description": "Demo UI Chain panel (build only)",
  "scripts": {
    "build": "esbuild src/Panel.tsx --bundle --format=esm --outfile=dist/panel.js --jsx=automatic --external:react --external:react/jsx-runtime"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
