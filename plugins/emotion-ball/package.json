{
  "name": "polaris-emotion-ball-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Polaris Emotion Ball v2 plugin — 32 SVG states, AI chat integration",
  "scripts": {
    "build": "esbuild src/Panel.tsx --bundle --format=esm --outfile=dist/panel.js --jsx=automatic --external:react --external:react/jsx-runtime"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}