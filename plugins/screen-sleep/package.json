{
  "name": "polaris-screen-sleep-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Polaris Screen Sleep plugin — Windows 一键熄屏",
  "scripts": {
    "build": "esbuild src/Panel.tsx --bundle --format=esm --outfile=dist/panel.js --jsx=automatic --external:react --external:react/jsx-runtime"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}